# Design Guide

> **Screen Wakelock Detector** uses project-specific Material Design 3 tokens.

Full design system: **[DESIGN_SYSTEM.md](DESIGN_SYSTEM.md)** — colors, typography, components, dynamic color, accessibility.

Implementation: `app/src/main/java/com/screenwakelock/detector/ui/theme/`.

The template `design-tokens/design-tokens.json` stub exists for bootstrap gate compatibility only; **do not sync** into production theme without an explicit migration task.
