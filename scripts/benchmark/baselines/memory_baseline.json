{
  "version": "1.0",
  "description": "Fallback template when no device-specific baseline exists. First run on new hardware seeds baselines/devices/{MODEL}.json.",
  "pss_kb": 85000,
  "java_heap_kb": 12000,
  "delta_percent_max": 8,
  "delta_mb_max": 15,
  "monotonic_window": 3,
  "notes": "Template only — compare against baselines/devices/{ro.product.model}.json when present."
}
